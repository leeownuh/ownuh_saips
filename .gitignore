# ── Secrets — NEVER commit these ─────────────────────────────
backend/config/.env
keys/private.pem
keys/public.pem
keys/htdocs.lnk

# ── Logs ─────────────────────────────────────────────────────
*.log
logs/*.log
/var/log/

# ── Debug / temp files ───────────────────────────────────────
debug.php
debug_*.php
summarized_conversations/
*.swp
*.swo
backend/data/*.mmdb
composer.phar
composer-setup.php

# ── OS / IDE ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/

# ── PHP vendor is committed for portability (no composer needed on server) ──
# vendor/   ← intentionally kept

# ── Compiled assets kept as-is ───────────────────────────────
# assets/   ← intentionally kept
